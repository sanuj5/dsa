package algorithm.array;

/**
 * https://neetcode.io/problems/anagram-groups
 * Create map of string key and list of anagrams
 *  key --> String value of 26 char array.. if char matches --> update array[i -'a']++
 *  if key doesn't exist in map, initialize new List
 *  add current key, string to map
 */
public class AnagramGroups {
}
