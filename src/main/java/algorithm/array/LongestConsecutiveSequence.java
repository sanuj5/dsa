package algorithm.array;

/**
 * https://neetcode.io/problems/longest-consecutive-sequence
 * Create set and add all numbers
 * Iterate over set
 *      if previous number already exist --> continue
 *      count all number in set for increament of 1
 *      Fina current length vs max length
 *  Retun max length
 */
public class LongestConsecutiveSequence {
    public int longestConsecutive(int[] nums) {
        return 1;
    }
}
