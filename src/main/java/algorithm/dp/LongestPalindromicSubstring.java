package algorithm.dp;

/**
 * https://neetcode.io/problems/longest-palindromic-substring
 * Odd length search
 * For each element in array check if left == right , add to maxSubstring
 * Even length search
 * Maintain two pointer l=i, r=i+1
 * Check if equal and within range, increase both, add to maxSubstring
 */
public class LongestPalindromicSubstring {
}
